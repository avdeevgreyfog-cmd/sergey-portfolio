{
  "name": "sergey-developer-portfolio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "node scripts/clean.mjs",
    "typecheck": "tsc --noEmit",
    "build": "npm run clean && tsc && node scripts/build.mjs",
    "serve": "python3 -m http.server 4173 -d dist",
    "test": "python3 tests/qa.py"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
